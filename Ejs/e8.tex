\item Probar que si $\lambda$ es un autovalor de una isometría $S\in L(V)$ entonces $|\lambda|=1$. ¿Cuáles son los valores posibles para el determinante de $S$?
    \begin{mdframed}[style=s]
        
    \end{mdframed}